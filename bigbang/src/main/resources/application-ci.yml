# ==================== CI 环境 ====================
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_manage?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: BigBang@2026
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

  flyway:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create-drop   # CI 环境每次重建表
    show-sql: false

debug: false
